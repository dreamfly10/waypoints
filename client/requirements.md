## Packages
framer-motion | Page transitions and animations
recharts | Dashboard charts for readiness score
date-fns | Formatting dates for vault and community
clsx | Standard utility for conditional classes
tailwind-merge | Standard utility for tailwind classes

## Notes
- Assume user is authenticated implicitly for this prototype.
- API endpoints are mocked in the backend via shared/routes.ts structure.
- The Advisor feature returns 403 for advanced queries if `isPro` is false on the profile.
